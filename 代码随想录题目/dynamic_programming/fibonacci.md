# 斐波那契数 — Fibonacci Number

- **题目（LeetCode 509）**： [🔗 题目](https://leetcode.cn/problems/fibonacci-number/description/)  
- **难度**：简单
- **解析 / 学习链接**：  
  - [🧠 文字解析（代码随想录/Carl）](https://www.google.com/search?q=https://programmercarl.com/0509.%25E6%2596%2590%25E6%25B3%25A2%25E9%2582%25A3%25E5%25A5%2587%25E6%2595%25B0.html%23%25E7%25AE%2597%25E6%25B3%2595%25E5%2585%25AC%25E5%25BC%2580%25E8%25AF%25BE)  
  - [🎥 视频讲解（Bilibili）](https://www.bilibili.com/video/BV1f5411K7mo?vd_source=7923b10dbf11c28879c337d8e0bfa8de&spm_id_from=333.788.videopod.sections)

---
## 关键点（精简）
* 动态规划一般需要5步分析：
    1. **确定`dp`数组的含义**: `dp[i]`表示第i个斐波那契数的值
    2. **确定递推公式**: `dp[i]` = `dp[i-1]` + `dp[i-2]`
    3. **确定如何初始化**: dp[0] = 0, dp[1] = 1
    4. **遍历顺序**：从前向后
    5. **打印数组**：这一步是验证我们写的没问题，看看和想象是否相符

---

## 代码实现

```cpp
class Solution {
public:
    int fib(int n) {

        //计算边界条件 n=0 时
        if(!n) return 0; 

        //非边界条件
        //dp数组初始化
        vector<int> dp(n+1); 
        dp[0] = 0;
        dp[1] = 1; 

        //递推公式
        for(int i = 2; i < n+1; ++i){

            dp[i] = dp[i-1] + dp[i - 2];

        }

        return dp[n]; 

    }
}; 
```
* 时间复杂度: O(n)
* 空间复杂度: O(n)
* 当然不难发现，其实我们还可以维护三个变量就可以了
* 通过这样的想法，我们还可以简化：

```cpp
class Solution {
public:
    int fib(int n) {

        //计算边界条件 n=0 时
        if(!n) return 0; 

        //非边界条件
        //初始化
        int i = 0; //fib(0) = 0
        int j = 1; //fib(1) = 1

        //递推公式
        for(int iter = 2; iter < n+1; ++iter){

            int sum = i + j; 
            i = j; 
            j = sum; 

        }

        return j; 
        
    }
}; 
```
* 时间复杂度: O(n)
* 空间复杂度: O(1)
